
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}

  return Response.json({ success: true });
}
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}
import { logout } from "@/lib/auth";

export async function POST() {
  await logout();
  return Response.json({ success: true });
}
